% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_functions.R
\name{merge_three_tables}
\alias{merge_three_tables}
\title{Merge Three Data Tables}
\usage{
merge_three_tables(x, y, z, by_xy, by_xyz, all_xy = TRUE, all_xyz = TRUE)
}
\arguments{
\item{x}{The first data table.}

\item{y}{The second data table.}

\item{z}{The third data table.}

\item{by_xy}{A character vector specifying the column(s) to use for merging
`x` and `y`.}

\item{by_xyz}{A character vector specifying the column(s) to use for merging
the result of `(x, y)` with `z`.}

\item{all_xy}{A logical value indicating whether to perform a full outer join
for the first merge (x and y).  Defaults to TRUE.}

\item{all_xyz}{A logical value indicating whether to perform a full outer
join for the second merge (xy_merged and z). Defaults to TRUE.}
}
\value{
A merged data table resulting from the two merge operations.
}
\description{
This function takes three data tables (or data frames) and performs two
sequential merge operations.
}
\examples{
# Create sample data tables
students <- data.table::data.table(id = c("A", "B", "C", "D"),
                       Birthdate = c("2001-08-04", "2002-04-28", "2002-06-13", "2002-02-09"))
scores <- data.table::data.table(id = c("B", "C", "E"),
                     homework = c(87, 94, 92),
                     quiz = c(91, 90, 87))
grades <- data.table::data.table(id = c("B", "C", "E", "F"),
                     final_grade = c("A", "B", "C", "D"))

# Perform the merge
merged_data <- merge_three_tables(students, scores, grades, by_xy = "id", by_xyz = "id")
print(merged_data)

# Example with different join types
merged_data_inner <- merge_three_tables(students, scores, grades,
                                       by_xy = "id", by_xyz = "id",
                                       all_xy = FALSE, all_xyz = FALSE)
print(merged_data_inner)

}
